# Variables utilisées par docker-compose (copier vers .env à la racine).

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me-strong-password
POSTGRES_DB=incident_entry

# Fournisseur d'identité Alfresco ACS (URL vue par le conteneur backend)
ALFRESCO_BASE_URL=https://alfresco.company.com

# Si Alfresco tourne sur l'hôte ou une VM non joignable depuis Docker
# (ex. VM libvirt 192.168.122.x), activer le relais :
#   docker compose --profile alfresco-proxy up -d
# ALFRESCO_UPSTREAM_HOST=192.168.122.20
# ALFRESCO_UPSTREAM_PORT=8080
# ALFRESCO_PROXY_LISTEN_PORT=8081
# ALFRESCO_BASE_URL=http://host.docker.internal:8081

# Sessions — clé AES-256 (32 octets). Générer avec : openssl rand -hex 32
SESSION_ENCRYPTION_KEY=

# Cookie de session
SESSION_COOKIE_NAME=app_session
SESSION_DURATION_HOURS=8
# false = Docker HTTP local ; true = production HTTPS
SESSION_COOKIE_SECURE=false

# Sous-chemin de montage derrière un reverse proxy (vide = racine).
# Exemple : /request-buat pour servir sous archivage.buaa.dz/request-buat
APP_BASE_PATH=

# URL publique d'accès à l'application (origine vue par le navigateur).
# Exemple réseau local : http://192.168.1.26
# Exemple reverse proxy : https://archivage.buaa.dz
PUBLIC_APP_URL=http://localhost

# CORS : doit correspondre exactement à l'origine du navigateur.
CORS_ORIGIN=http://localhost

# Port HTTP exposé par Nginx
NGINX_HTTP_PORT=80

# Niveau de log backend
LOG_LEVEL=info
